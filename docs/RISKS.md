# Risks and limitations log

| Risk                                                             | Current mitigation                                                                                                               | Status                                                              |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| Webpage tricks field detection into choosing an unrelated input  | Scored signals, visibility/enabled checks, conservative thresholds                                                               | Unit and installed-Chrome tests pass                                |
| Lookalike checks miss a homograph                                | Exact registrable-domain policy plus limited controlled skeleton checks; honest non-goal                                         | Controlled fixtures pass; residual risk remains                     |
| Model hallucinates a domain, code, or Capsule fact               | Strict structured output, verbatim evidence validation, deterministic fallback; model never authorizes                           | Mocked malformed/invented-evidence tests pass                       |
| Extension leaks a temporary value after use                      | Masked UI, bounded memory, session replay ID without value, clear after action/expiry                                            | Storage, masking, and packaged-history tests pass                   |
| Local service is exposed cross-origin                            | Loopback binding, paired capability, extension-ID binding, CORS checks, bounded request size                                     | Unit and packaged-extension pairing tests pass                      |
| Judge machine lacks Playwright browser                           | Automated suite uses installed Chrome plus documented manual flow                                                                | Chromium needed only for contributor verification                   |
| Published extension does not match its version tag               | Tag/package-version check, tagged checkout, full verification, checksum                                                          | Enforced by GitHub Release workflow                                 |
| Gmail restricted-scope review blocks distribution                | Local test-user setup, read-only scope, loopback processing, explicit documentation                                              | Personal testing supported; Google verification remains             |
| OAuth token is exposed or retained unexpectedly                  | PKCE/state, OS keychain, pairing, disconnect/revoke, no token logging                                                            | Persistence, fallback, and connector tests pass                     |
| Provider returns excessive or malformed mailbox data             | One-day window, result caps, verification-like filtering, schema normalization                                                   | Gmail and Outlook adapter tests pass                                |
| Imported MIME is oversized, deeply nested, or active             | 2 MB cap, parser depth/header limits, inert extraction, no rendering or attachment extraction                                    | Plain, HTML-only, invalid, and oversized tests pass                 |
| OAuth callback or service port is misconfigured                  | Runtime-derived doctor checks callbacks, settings, ports, and private config mode                                                | CLI and installed-package doctor checks pass                        |
| First-time provider setup is error-prone                         | Guided callback/scope derivation; bounded Gmail JSON import; validated preserved config                                          | Gmail and Outlook clean-install setup smoke passes                  |
| Gmail secret leaks through shell history                         | Downloaded-JSON import writes owner-only config and never prints credentials                                                     | Valid/mismatch/no-echo tests pass                                   |
| Personal Outlook account cannot own Entra registration           | CLI/UI/docs distinguish app ownership from supported sign-in and explain tenant alternatives                                     | Live `AADSTS50020` reproduced; prerequisite explicit                |
| Inspecting a magic link consumes its token                       | URL-string parsing only; no fetch, HEAD, preconnect, prefetch, redirect resolution, image load, or clipboard                     | Inert-confirmation tests pass                                       |
| Magic link hides a different destination                         | HTTPS/registrable-domain policy; reject credentials, IP/local, ports, IDN, shorteners, redirects, mismatch                       | Unsafe/mismatch/lookalike/replay cases pass                         |
| Auto mode acts on a changed or wrong tab                         | Exact-origin opt-in, visible countdown, URL/intent/permission/policy revalidation, captured-tab action only                      | Same-tab, lookalike, cancel, and revoke tests pass                  |
| Page hides/removes the countdown                                 | Execution requires the closed-shadow host to remain connected and visible; otherwise cancel                                      | Overlay fail-closed test passes                                     |
| SPA wait dialog appears after load                               | Debounced top-level DOM observer scans only when supported intent changes                                                        | Packaged late-dialog Assisted test passes                           |
| Multiple recent messages are plausible                           | Different recent allowed values within 90 seconds block automation and require Manual selection                                  | Competing-message test passes                                       |
| Exact-site permission survives revocation                        | Rules retain protocol/host/port; settings remove exact rule and matching optional permission                                     | Exact-origin unit and packaged revoke tests pass                    |
| Activity history retains private message data                    | Strict minimal schema; no values/URLs/subjects/senders/bodies/paths; 24-record cap and seven-day TTL                             | Secret-field rejection and expiry tests pass                        |
| OTP input event causes site-owned submission                     | Auto consent warns; extension invokes no submit API; zero-submit fixture verifies extension behavior                             | Residual destination behavior documented                            |
| Reference detector fills unrelated field                         | Narrow grammar plus explicit booking/application/support labels and domain policy                                                | Unrelated field and submit remain unchanged                         |
| Provider HTML strips action destination                          | Gmail normalization preserves inert HTTPS anchor text without rendering or fetching                                              | HTML-only adapter test passes                                       |
| Real Gmail format differs from fixtures                          | Mixed code/link precedence, bounded long URLs, inert HTML links, expanded action phrasing                                        | Real Gmail-to-Medium same-tab handoff completed                     |
| Fallback code leaks through mixed-action subject                 | Confirmation masks action-code patterns and exact values                                                                         | Regression test passes                                              |
| Capsule leaks or over-collects message data                      | Closed two-fact schema, source-evidence checks, value/subject masking, no persistence or clipboard                               | Unit and browser regressions pass; destination sees approved facts  |
| Malicious labels or decoys confuse Capsule mapping               | Reject sensitive, hidden, disabled, zero-size, offscreen, ambiguous, nonempty, and split-container targets                       | Adversarial mapping tests pass                                      |
| Page listener causes partial Capsule mutation                    | Snapshot, native setter, post-set verification, reverse rollback, no success receipt on mismatch                                 | Framework-rewrite rollback test passes                              |
| Capsule overwrites user input                                    | Both targets must be empty during planning and again at action time                                                              | Nonempty fixture blocks                                             |
| Undo accidentally permits replay                                 | Capsule ID is marked used before receipt; Undo restores fields but leaves replay marked                                          | Unit and packaged replay tests pass                                 |
| Capsule outlives its temporary purpose                           | 90-second expiry, action-time policy, memory-only facts, dismissal/failure cleanup                                               | Fixed-clock expiry and stale-message tests pass                     |
| Arbitrary loopback page mounts Capsule UI                        | Exact 4173/4179 origin/root allowlist plus closed scenario/host/service tuples                                                   | Negative activation unit and packaged-browser tests pass            |
| Historical Gmail lookup broadens the ordinary one-day window     | Exact easyJet route, subject query, Spam/Trash exclusion, caps, strict evidence, masked user choice                              | Purpose-query, stale-window, and private conformance tests pass     |
| Forwarded easyJet mail makes an iCloud relay look authoritative  | Require `icloud.com` envelope plus exact encoded original-domain match; never trust display name alone                           | Valid-relay and forged-relay regressions pass                       |
| easyJet adds a consent control beside destination fields         | Map text/search inputs only with strong fact labels; never mutate checkboxes or invoke form actions                              | Live DOM inspection and exact-contract no-submit/Undo tests pass    |
| easyJet greeting contains a given name, not the required surname | Require an explicit surname/last-name label for two-fact Capsules; otherwise fill only the verified reference                    | Greeting false-positive and private reference-only regressions pass |
| Generic code scan selects unrelated mail on an Alaska page       | Exact Alaska route switches to a dedicated sender/subject-bounded booking query; strict message validation runs again locally    | Tennis Express exclusion and packaged Alaska selection tests pass   |
| Alaska traveler name is ambiguous or contains several passengers | Extract only one two-part name from the labeled `Traveler(s)` block; all other shapes fail closed rather than guessing a surname | Single traveler, multi-traveler, and unlabeled-name tests pass      |
