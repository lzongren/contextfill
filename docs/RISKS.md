# Risks and limitations log

| Risk                                                               | Current mitigation                                                                                             | Status                                                             |
| ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Webpage tricks field detection into choosing an unrelated input    | Scored OTP signals, visibility/enabled checks, conservative thresholds                                         | Unit and installed-Chrome tests pass                               |
| Lookalike checks miss a homograph                                  | Exact registrable-domain policy plus limited controlled skeleton checks; honest non-goal                       | Controlled fixtures pass; residual risk remains                    |
| Model hallucinates a domain or code                                | Strict structured output, evidence validation, deterministic fallback, model never authorizes                  | Mocked malformed/invented evidence tests pass; live no-key release |
| Extension leaks a code after use                                   | Short-lived memory only, masked display, session replay ID without value, clear after action/expiry            | Storage schema and packaged history tests pass                     |
| Local service is exposed cross-origin                              | Loopback binding, paired capability, extension-ID binding, CORS checks, bounded request size                   | Unit and packaged-extension pairing tests pass                     |
| Judge machine lacks Playwright browser                             | Automated suite uses installed Chrome plus documented manual flow                                              | Chromium install needed only for contributor verification          |
| Published extension does not match its version tag                 | Exact tag/package-version check, tagged checkout, full verification, generated checksum                        | Enforced by the GitHub Release workflow                            |
| Gmail restricted-scope review blocks general distribution          | Local test-user setup, read-only scope, loopback-only processing, explicit documentation                       | Personal testing supported; Google verification remains            |
| OAuth token is exposed or retained unexpectedly                    | PKCE/state, OS keychain, paired extension capability, disconnect/revoke, no token logging                      | Persistence, fallback, and connector tests pass                    |
| Provider returns excessive or malformed mailbox data               | One-day window, strict result caps, verification-like filtering, schema normalization                          | Gmail and Outlook adapter tests pass                               |
| Imported MIME is oversized, deeply nested, or contains active HTML | 2 MB file cap, parser depth/header limits, inert text extraction, no HTML rendering or attachment extraction   | Plain, HTML-only, invalid, and oversized import tests pass         |
| OAuth callback or service port is misconfigured                    | Runtime-derived `--doctor` checks exact callbacks, required settings, port equality, and private `.env` mode   | Focused CLI and installed-package doctor checks pass               |
| First-time provider setup is error-prone                           | Guided flows derive callbacks/scopes; Gmail imports downloaded JSON; config is validated and preserved         | Gmail and Outlook clean-install guided setup smoke passes          |
| Gmail client secret leaks through copy/paste or shell history      | Bounded downloaded-JSON import writes owner-only config and never prints credentials                           | Valid/mismatch/no-echo tests and installed-package smoke pass      |
| Personal Outlook account cannot own an Entra app registration      | CLI/UI/docs distinguish app ownership from supported sign-in audience and explain tenant-role alternatives     | Live `AADSTS50020` reproduced; prerequisite is now explicit        |
| Inspecting a magic link consumes its one-time token                | URL-string parsing only; no fetch, HEAD, preconnect, prefetch, redirect resolution, image load, or clipboard   | Unit and packaged-extension inert-confirmation tests pass          |
| Magic link hides a different destination or unsafe URL             | HTTPS/registrable-domain requirement; block credentials, IP/local, ports, IDN, shorteners, redirects, mismatch | Ten unsafe/mismatch/lookalike/replay cases pass                    |
| Auto mode opens a link on a changed/wrong tab                      | Exact-origin opt-in, visible countdown, current URL/page intent/policy revalidation, captured-tab update only  | Packaged same-tab, lookalike, cancel, and revoke tests pass        |
| Page hides/removes the countdown to suppress user control          | Countdown executes only while the closed-shadow host remains connected and visibly rendered; otherwise cancel  | Overlay fail-closed unit test passes                               |
| SPA verification dialog appears after initial page load            | Debounced top-level DOM signal observer triggers a fresh scan only when supported intent changes               | Packaged late-dialog Assisted test passes                          |
| Multiple recent messages are both plausible                        | Different recent allowed values within 90 seconds block automation and require Manual selection                | Competing-message unit test passes                                 |
| Exact-site permission remains after revocation                     | Rules retain protocol/host/port; settings remove only that exact rule and matching optional origin permission  | Exact-origin unit and packaged revoke tests pass                   |
| Activity history retains codes, tokens, or private message text    | Strict schema rejects extra fields; only site/type/outcome/reason/time; 24-record cap and seven-day TTL        | Secret-field rejection and expiry tests pass                       |
| OTP input event causes the destination site to submit              | Auto consent warns explicitly; extension clicks/calls no submit API; zero-submit fixture asserts its behavior  | Residual site-owned behavior documented                            |
| Reference detector fills an unrelated field                        | Narrow reference grammar plus explicit booking/application/support field labels and domain policy              | Generic order field ignored; unrelated field and submit unchanged  |
| Real provider HTML strips the action destination                   | Gmail HTML normalization preserves inert HTTPS anchor href text without rendering or fetching                  | HTML-only mailbox adapter test passes                              |
| A real Gmail magic-link format differs from fixtures               | Mixed code/link handling, long-URL bounding, inert HTML link preservation, and expanded action phrasing        | Real Gmail-to-Medium same-tab handoff completed                    |
| A fallback code leaks through a mixed-action email subject         | Confirmation presentation masks action-code patterns and exact candidate values before rendering               | Real Medium issue reproduced; regression test passes               |
